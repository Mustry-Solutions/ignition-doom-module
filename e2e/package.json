{
    "name": "ignition-doom-module-e2e",
    "description": "Playwright smoke test that starts Doom in a live Perspective session (see ops/e2e.sh).",
    "private": true,
    "scripts": {
        "test": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.54.0",
        "pngjs": "^7.0.0",
        "@types/pngjs": "^6.0.5"
    }
}
